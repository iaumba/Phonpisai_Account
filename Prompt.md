# ระบบบันทึกการใช้รถ โพนพิสัย ผ่าน LINE Messaging API (รองรับ AppSheet)

คุณคือนักพัฒนา Google Apps Script ผู้เชี่ยวชาญ มีหน้าที่เขียนและปรับปรุงฟังก์ชัน
บันทึกรายการใช้รถ ผ่าน LINE Messaging API เชื่อมต่อ Google Sheets
โดยออกแบบโครงสร้างข้อมูลให้รองรับการทำ **Web Dashboard ผ่าน AppSheet** ในภายหลัง

## [หลักการออกแบบเพื่อ AppSheet]
- ทุกตารางต้องมี **คอลัมน์ Key ที่มีค่าไม่ซ้ำ** (Primary Key) ได้แก่ `event_id`, `line_id`, `message_id`
- คอลัมน์ตัวเลขต้องเก็บเป็น **ตัวเลข raw** และ **เว้นว่าง** (blank) เมื่อไม่มีข้อมูล
  (ห้ามใส่ '-' หรือ 'ไม่ระบุ' ลงในคอลัมน์ตัวเลข มิฉะนั้น AppSheet จะเดาประเภทคอลัมน์ผิดเป็นข้อความ)
- วันที่/เวลา ต้องเขียนเป็น **Date object จริง** (Timezone Asia/Bangkok)
- ชื่อคอลัมน์เป็น **ภาษาอังกฤษแบบ underscore** (เช่น `event_id`, `event_time`, `fuel_after_pct`, `receipt_url`)
  เพื่อให้ AppSheet ตั้ง Keys/References/index ได้เสถียร
- ชีท `Log` เป็นชีทระบบ **ห้ามสร้างตารางใน AppSheet** (ไปปิดใน Data > Tables)
- ความสัมพันธ์ Tables ใน AppSheet: `sender_line_id` → `ผู้รับผิดชอบรถ.line_id`, `event_id` → `เติมน้ำมัน.event_id`

## [ตรรกะการรับรูป - เหตุการณ์ทั้ง 3 แบบ]
1. ออกร้าน: รูปเลขไมล์ 1 รูป มักส่งก่อนเที่ยง
2. เติมน้ำมัน: รูปเกจก่อนเติม + รูปเกจหลังเติม + รูปบิลน้ำมัน (ส่งพร้อมกัน 3 รูป หรือใกล้เคียงกัน
   บางครั้งไม่ครบทั้ง 3 รูป)
3. เข้าร้าน: รูปเลขไมล์ 1 รูป มักส่งหลังเที่ยง

## [โครงสร้าง Google Sheets] (GOOGLE_SHEET_ID จาก ScriptProperties)

### ชีท: ผู้รับผิดชอบรถ (ตารางอ้างอิง - Ref)
| line_id | name | plate |
|--------|------|-------|

### ชีท: เหตุการณ์ (ตารางหลัก - ทุกเหตุการณ์ เข้า/ออก/เติมน้ำมัน)
| column | type | หมายเหตุ |
|--------|------|---------|
| event_id | text (KEY) | UUID จาก Utilities.generateUUID() |
| timestamp | datetime | เวลาที่ LINE ส่ง (จริง) |
| event_time | datetime | เวลาที่เกิดเหตุ: เข้า/ออก = timestamp, เติมน้ำมัน = เวลาบิล (ถ้าอ่านได้) |
| event_type | text | เข้า / ออก / เติมน้ำมัน |
| sender_line_id | text (ref) | userId ของผู้ส่ง |
| plate | text (ref) | ทะเบียนรถที่สันนิษฐานได้ |
| mileage_km | number | เลขไมล์ อ่านจากมิเตอร์ (ไม่คำนวณระยะทาง) |
| error_note | text | blank เมื่อไม่มี error |

### ชีท: เติมน้ำมัน (ตารางรายละเอียด - ref ด้วย event_id)
| column | type | หมายเหตุ |
|--------|------|---------|
| event_id | text (KEY, ref) | ตรงกับ event_id ในชีท เหตุการณ์ |
| fuel_before_pct | number | % ก่อนเติม (0-100) ไม่มีทศนิยม, blank ถ้าไม่ได้ |
| fuel_after_pct | number | % หลังเติม, blank ถ้าไม่ได้ |
| amount_baht | number | จำนวนเงิน |
| price_per_liter | number | ราคา/ลิตร |
| liters | number | จำนวนลิตรที่ได้ |
| district | text | อำเภอของปั้ม |
| receipt_url | text | URL ภาพบิลใน Drive |
| error_note | text | blank เมื่อไม่มี error |

### ชีท: Log (ชีทระบบ - ไม่สร้างตารางใน AppSheet)
| message_id | sender_line_id | event_id | status | error_log | received_at |
|-----------|----------------|----------|--------|-----------|-------------|
| (KEY) | userId ผู้ส่ง (ใช้จับกลุ่มรูป) | UUID ของ Event | queued / done / error | | |

หมายเหตุ: `sender_line_id` ใช้สำหรับจับกลุ่มภาพจากคนเดียวกันที่ส่งในเวลากล้อกันให้เป็นเหตุการณ์เดียวกัน
(Log ไม่สร้างตารางใน AppSheet)

## [การระบุรถคันที่ใช้] (เรียงลำดับ)
1. หา lineID ในชีท "ผู้รับผิดชอบรถ" -> ได้ทะเบียนรถของคนนั้น (รถประจำ)
2. ถ้าผู้ขับไปขับคันอื่น: เปรียบเทียบเลขไมล์ที่ส่งกับประวัติเลขไมล์ของแต่ละคัน
   เลือกคันที่เลขไมล์ "สอดคล้อง" (มีแนวโน้มต่อเนื่องจากเลขไมล์ล่าสุดของคันนั้น)
   โดยให้เชื่อเลขไมล์มากกว่า "ทะเบียนรถประจำตัวผู้ขับ"

## [การแยกเหตุการณ์ เข้า/ออก] (ทางเลือก ก.)
- ใช้เลขไมล์เทียบกับเรคคอร์ดล่าสุดของคันนั้น: ถ้าเรคคอร์ดล่าสุดคือ "เข้า" เลขไมล์ใหม่ที่เพิ่มขึ้น
  = "ออกร้าน" และสลับกันในทางกลับกัน
- ถ้าอ่านเลขไมล์ไม่ได้หรือข้อมูลไม่พอ -> ให้ fallback ใช้เวลาส่ง (ก่อนเที่ยง=ออก, หลังเที่ยง=เข้า)
  และระบุ error

## [การดักรายการซ้ำ (duplicate)]
- เก็บ messageId ของทุก event ในชีท Log ก่อนประมวลผล
- ถ้า messageId เคยถูกประมวลผลแล้ว -> ข้ามทันที

## [การจัดการรูปบิลน้ำมัน] (เฉพาะบิลเติมน้ำมันเท่านั้น)
- ดาวน์โหลดรูปบิล -> คัดลอกไป Google Drive โฟลเดอร์ FOLDER_ID
- เปลี่ยนชื่อไฟล์: ทะเบียนรถ_ปีค.ศ.เดือนวัน_ชั่วโมงนาที เช่น 6952_20260923_1539
- บันทึก URL ของไฟล์ลงคอลัมน์ receipt_url

## [การอ่านค่าจากรูปด้วย Gemini]
- ใช้โมเดล gemini-3.5-flash-lite ผาง Google API Key (ตรวจสอบชื่อรุ่นจริงก่อน deploy)
- สั่งให้ Gemini ตอบเป็น JSON ระบุ: ประเภทภาพ (odometer / fuel_gauge / receipt),
  เลขไมล์, %น้ำมัน (ตำแหน่งเข็มระหว่าง F-E, เต็มจำนวนไม่ทศนิยม), ข้อมูลบิล
  (เวลาวางมือจ่าย, จำนวนเงิน, ราคา/ลิตร, จำนวนลิตร, อำเภอปั้ม, ทะเบียนถ้ามี)
- ถ้าได้รับรูปเกจเข็มเพียง 1 รูปให้ถือว่าเป็น "หลังเติม" (fuel_after_pct)
- เลขไมล์: อ่านค่าจากมิเตอร์รถเท่านั้น ไม่ต้องคำนวณระยะทาง

## [วันที่-เวลา]
- เข้า/ออก: ใช้เวลาตาม LINE (timestamp)
- เติมน้ำมัน: ใช้เวลาบนใบเสร็จ (เวลาวางมือจ่าย) เป็นหลัก พยายามแปลงเป็น ค.ศ.
  (ให้สอดคล้องกับชื่อไฟล์รูป) ถ้าอ่านไม่ได้ -> ใช้เวลาตาม LINE
- ทุกครั้งแปลง timezone เป็น Asia/Bangkok และเขียนเป็น Date object

## [การบันทึกข้อมูล]
- บันทึกทุกเหตุการณ์ แม้อ่านข้อมูลไม่ได้ -> คอลัมน์ตัวเลขเว้นว่าง, คอลัมน์ข้อความใส่ 'ไม่ระบุ' หรือ blank,
  พร้อมระบุ error ใน error_note / error_log
- ไม่มีการ Reply/แจ้งเตือนกลับเข้าสาย LINE ทุกกรณี
- จำนวนเงิน/ราคา/ลิตร: บันทึกเป็นตัวเลข raw ไม่บังคับ format

## [ข้อกำหนดทางเทคนิค]
- ScriptProperties ที่ต้องตั้งค่า: LINE_ACCESS_TOKEN, GOOGLE_SHEET_ID, FOLDER_ID, GEMINI_API_KEY
- ค่า config ที่ปรับได้ผ่าน ScriptProperties (ไม่ตั้งก็ใช้ค่า default ในโค้ด):
  GEMINI_MODEL, WAIT_MINUTES, GAP_MINUTES, TZ, SHEET_VEHICLES, SHEET_EVENTS, SHEET_FUEL, SHEET_LOG
- webhook doPost(e) ไม่ต้อง Verify Signature ไม่ต้อง Whitelist (ผู้ใช้ไม่เกิน 10 คน, รถไม่เกิน 10 คัน)
- ตอบ HTTP 200 ให้เร็วที่สุด แล้วประมวลผลแบบ async ผ่าน Time-Driven Trigger (ทุก 1 นาที)
  เรียกฟังก์ชันประมวลผลที่มี LockService กันงานพร้อมกัน และกัน LINE retry event ซ้ำ
- ใช้ Utilities.generateUUID สำหรับ key ภายใน